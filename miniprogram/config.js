/**
 * 运行时配置。
 *
 * 放在这里而不是散落在各页面，是为了让「环境相关」和「业务相关」分开，
 * 换环境、换城市时只改这一个文件。
 */

module.exports = {
  // 云开发环境 ID。
  // 获取方式：微信开发者工具 → 工具栏「云开发」→ 新建环境 → 环境 ID
  // 建议建两个：dev（开发）与 prod（生产），这里填当前联调用的
  //
  // TODO: 建好环境后把下面的值换成真实环境 ID
  cloudEnv: '',

  // v1 首发城市。城市是数据字段，不是硬编码的常量，这里只是默认值
  defaultCity: '深圳',

  // 场次宽限期（分钟）：开始后多久内仍算「正在跑」。
  // 用于推导「此刻 N 人正在跑」，见 utils/session.js
  graceMinutes: 90,

  // 每天最多创建的队伍数（防刷）
  maxTeamsPerDay: 3,

  // 打卡录入手输时的合理区间，超出即视为误输入。
  // 注意：这只是在防误填，不是在审核用户是否真跑了（PRD 第 8 章）
  checkin: {
    maxKm: 200,
    maxDurationSeconds: 24 * 3600
  }
}
